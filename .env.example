# Optional: connect the app to Supabase for production authentication and data.
# Without these values, the app runs in a browser-only demo mode.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-public-anon-key
